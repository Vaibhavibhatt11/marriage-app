name: brahmin_matrimony
description: Brahmin Marriage Matrimonial Application
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.1
  cloud_firestore: ^4.15.1
  firebase_storage: ^11.3.1
  firebase_messaging: ^14.7.10

  # State Management
  riverpod: ^2.4.9
  flutter_riverpod: ^2.4.9

  # UI & Navigation
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.10+1
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  carousel_slider: ^4.2.1
  intl: ^0.19.0

  # Utilities
  flutter_dotenv: ^5.1.0
  dio: ^5.3.3
  shared_preferences: ^2.2.2
  uuid: ^3.0.7
  country_code_picker: ^2.0.2
  flutter_phone_number_field: ^1.0.0

  # Chat
  emoji_picker_flutter: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/svgs/
    - .env